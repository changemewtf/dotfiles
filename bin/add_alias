#!/bin/bash

NAME="$1"
shift
ALIAS="$@"

DOTFILE_DIR="$HOME/$(<$HOME/.dotfile_directory)"

case "$(basename $0)" in
add_alias)
  FILE=bash_aliases
  STR="alias $NAME='$ALIAS'"
  ;;
add_function)
  FILE=bash_functions
  STR="function $NAME { $ALIAS; }"
  ;;
esac

PATH="$DOTFILE_DIR/sh/$FILE"
